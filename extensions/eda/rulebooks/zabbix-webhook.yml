---
- name: Listen for Zabbix Alerts
  hosts: all
  sources:
    - ansible.eda.webhook:
        host: 0.0.0.0
        port: 5001
  rules:
    - name: Dump everything
      condition: event is defined
      action:
        debug:
          msg: "Raw event: {{ event }}"
